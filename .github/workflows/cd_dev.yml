# name: CD - Dev/QA

# on:
#   workflow_dispatch:

#   workflow_run:
#     workflows: [ "CI - Validate SQL" ]
#     types:
#     - completed
#     branches:
#     - dev

# jobs:
#   deploy-dev:
#     if: >
#       github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success'

#     runs-on: ubuntu-latest
#     environment: development

#     env:
#       SNOWFLAKE_ACCOUNT: ${{ secrets.SF_ACCOUNT_NAME }}
#       SNOWFLAKE_USER: ${{ secrets.SF_USER }}
#       SNOWFLAKE_PASSWORD: ${{ secrets.SF_PASSWORD }}
#       SNOWFLAKE_ROLE: ${{ secrets.SF_ROLE }}
#       SNOWFLAKE_WAREHOUSE: COMPUTE_WH
#       SNOWFLAKE_DATABASE: ${{ secrets.SF_DATABASE }}

#     steps:
#     - uses: actions/checkout@v4

#     - name: Set up Python
#       uses: actions/setup-python@v5
#       with:
#         python-version: '3.11'

#     - run: pip install schemachange snowflake-connector-python

#     - name: Create pre-deploy backup snapshot
#       id: backup
#       run: python scripts/backup_snapshot.py

#     - name: Record deploy start time
#       id: start_time
#       run: echo "ts=$(date -u +'%Y-%m-%d %H:%M:%S')" >> "$GITHUB_OUTPUT"

#     - name: Deploy to Dev
#       run: |
#         schemachange deploy -f objects \
#             --vars "{\"db\": \"$SNOWFLAKE_DATABASE\"}" \
#             --create-change-history-table

#     - name: Post-deployment validation
#       run: python scripts/validate_deployment.py "${{ steps.start_time.outputs.ts }}"

#     - name: Rollback on failure
#       if: failure() && steps.backup.outcome == 'success'
#       run: python scripts/rollback.py "${{ steps.backup.outputs.backup_db }}"

#     - name: Cleanup backup snapshot
#       if: success()
#       run: python scripts/cleanup_backup.py "${{ steps.backup.outputs.backup_db }}"

name: CD - Dev/QA

on:
  workflow_dispatch:

  workflow_run:
    workflows: [ "CI - Validate SQL" ]
    types:
    - completed
    branches:
    - dev

jobs:
  deploy-dev:
    if: >
      github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success'

    runs-on: ubuntu-latest
    environment: development

    env:
      SNOWFLAKE_ACCOUNT: ${{ secrets.SF_ACCOUNT_NAME }}
      SNOWFLAKE_USER: ${{ secrets.SF_USER }}
      SNOWFLAKE_PASSWORD: ${{ secrets.SF_PASSWORD }}
      # Same repo Variables terraform.yml's setup job reads for development —
      # single source of truth in Settings > Actions > Variables.
      SNOWFLAKE_ROLE: ${{ vars.APP_ROLE_DEV }}
      SNOWFLAKE_WAREHOUSE: ${{ vars.APP_WH_DEV }}
      SNOWFLAKE_DATABASE: ${{ vars.APP_DATABASE_DEV }}

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - run: pip install schemachange snowflake-connector-python

    - name: Create pre-deploy backup snapshot
      id: backup
      run: python scripts/backup_snapshot.py

    - name: Record deploy start time
      id: start_time
      run: echo "ts=$(date -u +'%Y-%m-%d %H:%M:%S')" >> "$GITHUB_OUTPUT"

    - name: Deploy to Dev
      run: |
        schemachange deploy -f objects \
            --vars "{\"db\": \"$SNOWFLAKE_DATABASE\"}" \
            --create-change-history-table

    - name: Post-deployment validation
      run: python scripts/validate_deployment.py "${{ steps.start_time.outputs.ts }}"

    - name: Rollback on failure
      if: failure() && steps.backup.outcome == 'success'
      run: python scripts/rollback.py "${{ steps.backup.outputs.backup_db }}"

    - name: Cleanup backup snapshot
      if: success()
      run: python scripts/cleanup_backup.py "${{ steps.backup.outputs.backup_db }}"
